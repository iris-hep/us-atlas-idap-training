[workspace]
channels = ["conda-forge"]
name = "agentic-workflows"
platforms = ["linux-64", "osx-arm64", "win-64"]
version = "0.1.0"

[tasks.create-config-files]
cmd = """
cp ./env-template .env && \
echo 'set secrets and user configs in generated files'
"""

[dependencies]
